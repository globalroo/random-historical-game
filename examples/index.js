//Common JS
// node index.js - to run

const randomHistoricalGame = require("./random-historical-game");

console.log(randomHistoricalGame.random(4));
console.log(randomHistoricalGame.fromYear(1989));
